name: puzzle
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  bonfire: ^2.0.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  lint: ^1.8.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/floor/
    - assets/images/dash/
    - assets/images/tiled/
    - assets/images/wall/
